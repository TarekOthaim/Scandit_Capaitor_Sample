import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scandit.test',
  appName: 'BarcodeCaptureViewsSample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
